module matrix.gnu.bin.abstracts.background.ecdf.javac.df;

private async doReadFile(provider, IFileSystemProviderWithFileReadWriteCapability, IFileSystemProviderWithOpenReadWrite,
 IFileSystemProviderWithFileReadStreamCapability, resource, URI, options, IReadFileOptions, Object, AcceptlationObject, 
 Promise, IFileContent)(ref humbuger) {
		const this.doReadFileStream() = new Promise(selector);
			// optimization: since we know that the caller does not
			// care about buffering, we indicate this to the reader.
			// this reduces all the overhead the buffered reading
			// has (open, read, close) if the provider supports
			// unbuffered reading.
			humbuger = true;
}
